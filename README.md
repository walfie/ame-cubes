# ame-cubes

## Development

```bash
yarn install
yarn dev
```

## Production

To build files to the `dist` directory:

```bash
yarn build
```

To build and deploy to GitHub pages:

```bash
yarn deploy
```
